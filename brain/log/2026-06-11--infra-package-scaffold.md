---
title: "infra-package-scaffold: SST app scaffold"
type: log
packages: [api]
tasks: [infra-package-scaffold]
summary: Created packages/infra — the SST (Ion) app scaffold wiring app name/region, stage-aware safety, and the aya-<stage> / aya-uploads-<stage> naming convention.
updated: 2026-06-11
---

# infra-package-scaffold

Created `@aya/infra` (`packages/infra/`): `sst.config.ts`, `package.json`,
`tsconfig.json`, `eslint.config.mjs`. SST v4 (Ion) as a dev dependency.

- `app()` sets `name: "aya"`, region from `AWS_REGION` (default `us-east-1`),
  `home: "aws"`; `prod` retains+protects, all other stages remove.
- `run()` establishes the naming convention `aya-<stage>` (table) and
  `aya-uploads-<stage>` (bucket) and returns them as outputs. No resources yet —
  downstream infra-* tasks add Dynamo/S3/API/Nextjs inside `run()`.
- `.sst/` (generated by `npx sst install`) added to root `.gitignore`.

**Verification.** typecheck + lint + build green across all packages (infra has no
build task — IaC). `sst diff --stage dev` synthesizes config and stops at the
AWS-credentials refresh — synthesis-only; real deploy needs the account in the
[aws-account handoff](../handoffs/aws-account.md) (updated, still open). No secrets
committed.

New page: [packages/infra](../packages/infra.md).
